# 实验总结与踩坑记录

> SAM-Road Completion 全程实验汇总。仅列结论与坑,不复述代码。

---

## 〇、任务设定

**多模态互补的路网补全(Road Network Completion)**

核心思想:**traj 是训练增强模态,不是推理输入**。

| 阶段 | traj | rn(已知路网) | 说明 |
|---|---|---|---|
| **训练** | ✅ 用 | ✅ 用(edge_random 随机采样 partial) | traj 让模型从轨迹学到路网形态,增强视觉特征;rn 用按边随机采样(训练稳定) |
| **推理** | ❌ 不用 | ✅ 用(component 不破碎 partial) | traj 可达性比 rsimg 差太多+处理复杂,实际场景不靠谱;任务从 extraction 变 completion |

**相比原版 SAM-Road 的转变**:推理从纯视觉 extraction 变成基于已知 rn 的 completion。traj 的价值在训练时增强,推理时省掉。

**要验证的东西**:
1. **traj 训练增强的价值**:有 traj 训练 vs 无 traj 训练(方案A vs B),同样 img+rn 推理
2. **rn 推理补全的价值**:traj 训练的模型,推理加 rn vs 不加 rn
3. **退化正确性**:completion 无先验(无traj无rn)≈ extraction,不劣化
4. **partial 策略**:edge_random 训练 + component 推理(训练稳定 + 真实不破碎)
5. **黑边对齐**:xian 裁剪对齐 sat 黑边后,train/eval/推理 GT 统一

**数据集**:
- **DiDi Xian**:有 traj(快递员 GPS),378 region(裁剪对齐后),可测全部目标
- **SpaceNet**:无 traj,只能测 extraction vs completion(rn-only)

---

## 一、最终结果(didi_xian,黑边裁剪对齐+epoch9,2026-06-29)

### 1.1 sam_road(traj 训练,ep9 ckpt,推理用 component partial)

| 档 | 训练 | 推理 | partial | APLS | TOPO F1 | TOPO P | TOPO R |
|---|---|---|---|:---:|:---:|:---:|:---:|
| extraction 基线 | img | img | — | 0.4211 | 0.6069 | 0.8948 | 0.4591 |
| 目标1(无traj无rn) | img+traj | img | — | 0.4667 | 0.6347 | 0.8689 | 0.5000 |
| trajonly(参考,偏离设定) | img+traj | img+traj | — | 0.4860 | 0.6541 | 0.8924 | 0.5163 |
| rn-only(**主设定**) | img+traj | img+rn | component | **0.5965** | **0.7602** | 0.8448 | **0.6909** |
| full(目标3) | img+traj | img+traj+rn | component | **0.6204** | **0.7714** | 0.8609 | **0.6987** |

### 1.2 P2CNet 对比(didi_xian,同数据集,公平对比:component partial,50% fixed)

| 方法 | 输入 | 输出形态 | APLS | TOPO F1 | TOPO P | TOPO R |
|---|---|---|:---:|:---:|:---:|:---:|
| **P2CNet**(Par 分支) | img+partial(component,50%) | mask→骨架提图 | **0.6720** | 0.6697 | **0.9597** | 0.5143 |
| **sam_road rn-only**(主设定) | img+rn(component,50%) | graph 原生 | 0.5965 | **0.7602** | 0.8448 | **0.6909** |
| **sam_road full** | img+traj+rn(component,50%) | graph 原生 | **0.6204** | **0.7714** | 0.8609 | **0.6987** |

> **公平对比条件**:同为 didi_xian 裁剪对齐数据,test 39 region,component partial(seed=42,thickness=2),keep_ratio=50% fixed。eval GT(graph_gt.pickle)均裁剪对齐 sat 黑边。
> P2CNet: 56 epoch early stop, best val mIoU=0.7593。test(50% fixed) mIoU=0.7379, road IoU=0.4956。
> P2CNet train 用 mix(25/50/75 增强),test 固定 50%(与 sam_road 一致)。

### 1.3 核心结论

#### sam_road 内部消融
1. **rn 推理补全价值巨大**:目标1(0.4667)→ rn-only(0.5965),APLS +0.130(+28%)。rn 是 completion 的核心先验
2. **traj 推理增量小且偏离设定**:rn-only(0.5965)→ full(0.6204),+0.024。traj 推理不是设定方向
3. **退化正确**:目标1(0.4667)> extraction(0.4211),completion 模型无先验时不劣于 extraction(traj 训练增强)
4. **recall 提升是 rn 的核心贡献**:extraction R=0.4591 → rn-only R=0.6909(+0.232),rn 大幅补全漏检路;precision 略降(0.895→0.845)但 F1 净赚

#### sam_road vs P2CNet(公平对比,50% fixed)
5. **APLS:P2CNet(0.6720)> sam_road rn-only(0.5965)**,但 < sam_road full(0.6204)差距缩小。P2CNet 像素级补全 + A* 连边,连通性由 mask 保证;sam_road TopoNet 逐边预测易断边
6. **TOPO F1:sam_road(0.7602/0.7714)≫ P2CNet(0.6697)**。sam_road graph 原生输出,拓扑精确性显著更高(+0.09~0.10)
7. **P/R 权衡**:P2CNet 极高 precision(0.960)低 recall(0.514,保守漏检);sam_road 更均衡(P≈0.85, R≈0.69),recall 远高于 P2CNet(+0.18)
8. **评测口径差异**:P2CNet mask→骨架提图(有转换损失,mask 断裂处图断路);sam_road graph 原生(无转换)
9. **范式差异**:P2CNet 像素级补全在连通性(APLS)上占优(mask 连续→A* 保证连通),sam_road graph 级在拓扑精确性(TOPO)和路网覆盖(recall)上占优

### 1.4 APLS 与 TOPO 指标本质解析

#### APLS(Average Path Length Similarity)

**计算方式**:在 GT 图上选取 control points(度≠2 的节点,即交叉口/端点),在 pred 图中用 10m 容差找对应节点。对每对对应节点,计算 GT 最短路径长度 d1 和 pred 最短路径长度 d2:

```
s = |d1 - d2| / d1     (归一化路径长度差异, 截断到[0,1])
APLS = 1 - mean(s)     (所有节点对的平均)
```

特殊情况:
- pred 中找不到对应节点(漏检,10m 内无节点)→ s=1(得0分,**参与计算,不是跳过**)
- pred 中两端都匹配到,但路径不可达(断边)→ d2=∞ → s=1(得0分)
- pred 路径比 GT 短(错连捷径)→ d2<d1 → s=|d1-d2|/d1 >0(**同样降分**)

**APLS 反映的是**:路网的**路径长度准确性**——不仅要求连通(断边得0分),还要求路径长度接近 GT(错连捷径也降分)。它是一个**综合性的拓扑指标**,同时惩罚断边、错连、漏检三种错误。

**P2CNet APLS 高的原因**:P2CNet 的 mask 补全质量好(mIoU 0.738),mask 连续 → A* 沿 mask 像素连边 → 连上的路路径长度和 GT 接近。虽然漏检多(recall 0.51,部分节点对得0分),但**连上的路的路径长度准确**,大量节点对 s 接近 0。

**sam_road APLS 低的原因**:TopoNet 逐边预测,该连的边可能预测概率 < 阈值 → 断边 → pred 路径变长或不可达 → s 增大。虽然覆盖更全(recall 0.69),但**断边导致路径长度偏离 GT**。

#### TOPO(Topological F1)

**计算方式**:采用"marble-hole matching"算法:
1. 在 pred 图上随机选节点(marble 源),在 GT 图上找 threshold 内最近节点(hole 源)
2. 从两个源节点各自沿图"走"一段(step 间隔采样,半径 r 内),产生两串采样点(marbles 和 holes)
3. **Precision**:pred 的 marbles 有多少能在 GT 的 holes 中找到匹配(threshold 内)→ pred 的路有多少落在 GT 上
4. **Recall**:GT 的 holes 有多少能在 pred 的 marbles 中找到匹配 → GT 的路有多少被 pred 覆盖
5. F1 = 2·P·R/(P+R)

**TOPO 反映的是**:路网的**空间覆盖精确性**——pred 的路有多少确实在 GT 附近(precision),GT 的路有多少被 pred 覆盖(recall)。它**不关心路径长度,只关心路的几何位置是否匹配**。

**sam_road TOPO F1 高的原因**:TopoNet 直接预测节点和边,节点位置由 mask NMS 提取(精确),边由 TopoNet 预测(覆盖全)。recall 0.69 远高于 P2CNet 0.51——发现更多路。

**P2CNet TOPO F1 低的原因**:mask → 骨架提取 keypoint → A* 连边,骨架提取有信息损失(节点位置不精确),且 mask 漏检多 → recall 低(0.51)。precision 高(0.96)因为连上的路位置准,但漏检多拉低 F1。

#### 两个指标的根本区别

| | APLS | TOPO |
|---|---|---|
| **衡量什么** | 路径长度准确性(连通性+长度) | 空间覆盖精确性(位置匹配) |
| **核心计算** | 节点对最短路径长度比 | 采样点位置匹配(marble-hole) |
| **断边** | 路径变长/不可达 → 重罚(s=1) | 该区域无 marbles → recall 降 |
| **错连捷径** | 路径变短 → 罚(s=|d1-d2|/d1) | 多余 marbles → precision 降 |
| **漏检** | 节点匹配不到 → s=1(得0分) | GT 路无 pred 覆盖 → recall 降 |
| **偏好** | 连通且路径长度准的图 | 覆盖全且位置准的图 |

**APLS 偏好"连通性"**:只要路连上且路径长度接近 GT,APLS 就高——即使漏检一些路(只要漏的不影响已连路的长度)。
**TOPO 偏好"覆盖性"**:只要路的位置在 GT 附近,TOPO 就高——即使路断了(只要断处的采样点仍能匹配)。

**这就是为什么 P2CNet 和 sam_road 在两个指标上各有胜负**:
- P2CNet 的 A* 连边保证连通(mask 连续→路径长度准)→ APLS 高,但骨架提取漏检多 → TOPO recall 低
- sam_road 的 TopoNet 覆盖全(发现更多路)→ TOPO recall 高,但断边多 → APLS 低

### 1.4 spacenet(无 traj,0625 旧结果,未裁剪)

| 档 | 推理 | APLS | TOPO F1 | TOPO P | TOPO R |
|---|---|:---:|:---:|:---:|:---:|
| extraction 基线 | img | 0.7012 | 0.7917 | 0.9290 | 0.6897 |
| 目标1(无rn) | img | 0.7011 | 0.7960 | 0.9298 | 0.6959 |
| rn-only(有rn) | img+rn | 0.7000 | 0.7964 | 0.9305 | 0.6961 |

> spacenet 无 traj,rn 增量归零(extraction 已强,rn 冗余)。

---

## 二、partial 采样策略实验

> 均为 didi_xian, epoch9 ckpt。前两行同一 ckpt(0.2246, edge_random 训练, 0628)只换推理 partial;
> 第三行换 component 训练 ckpt(0.2628, 0626)作对照。extraction baseline 同节取 clipped ep9。

| 训练采样 | 推理 partial | APLS | TOPO F1 | TOPO P | TOPO R | val_loss |
|---|---|:---:|:---:|:---:|:---:|---|
| **extraction baseline** | img(无 partial) | 0.4211 | 0.6069 | 0.8948 | 0.4591 | — |
| edge_random | edge_random | 0.5864 | 0.7724 | 0.8609 | 0.7003 | 平滑 |
| edge_random | component | 0.6204 | 0.7714 | 0.8609 | 0.6987 | 平滑 |
| component | component | 0.4571 | 0.6316 | 0.6903 | 0.5821 | **震荡** |

**结论**:
1. **vs baseline**:completion 三档(0.5864/0.6204/0.4571)APLS 均高于 extraction baseline(0.4211),但 component 训练档(0.4571)仅略高、F1(0.6316)反而低于 baseline(0.6069)以外的两档,说明 component 训练退化明显。
2. **edge_random 训练 + component 推理(0.6204)远好于 component 训练(0.4571)**。component 训练每 epoch 整块留/删导致震荡,F1 降 0.14、P 降 0.17。
3. **P/R 权衡**:edge_random 两档 P≈0.861 略低于 baseline(0.895),但 R≈0.70 远高于 baseline(0.459,+0.24)——partial 先验主要补 recall(漏检路);component 训练档 P/R 双降(0.690/0.582)。
4. **最终方案:训练 edge_random(稳定),推理 component(真实不破碎)**。

---

## 三、踩坑记录

### 坑1:坐标系 bug(completion 比 extraction 差的根因)
- known graph pickle 是 `(row,col)`,render/匹配/注入期待 `(x,y)`,漏了 coord_transform
- 修复:两个入口统一转 `(x,y)`,边端点正确率 0%→100%

### 坑2:component 采样训练震荡
- component 每 epoch 整块留/删,val_loss 0.2~1.5 震荡
- 修复:训练回退 edge_random;推理用 component

### 坑3:方案1 loss(known edge 标签 True)更差
- known edge 标签 True 成捷径监督,recall 降
- 修复:撤销,回退旧 loss

### 坑4:spacenet rn 增量为零
- extraction recall 0.69(强基线),rn 冗余。rn 价值与 extraction 基线强度负相关

### 坑5:partial 可复现性
- 全局 random 累积 → per-file `random.Random(42)`

### 坑6:Dropout 步 valid 未恢复
- dropout 清零先验后 valid 没更新 → 恢复 True

### 坑7:推理 dtype bug
- numpy float64 → torch float32

### 坑8:任务设定理解偏差(traj 角色)
- traj 是训练增强模态,推理不用

### 坑9:xian 黑边不匹配
- 38 个边缘 region sat 有黑边但 rn 有路。cohen-sutherland 裁剪 rn graph 对齐 sat,裁断处补 node

### 坑10:partial PNG thickness 不一致
- partial.png thickness 3→2,对齐 prepare_dataset 和 DelvMap

### 坑11:评测 GT 未裁剪 + 用了 epoch1 ckpt
- **症状**:extraction APLS 0.4288→0.2183 暴跌
- **根因1**:clip_rn_to_sat 只裁了 refine_gt_graph.p(训练GT),没裁 graph_gt.pickle(评测GT) → 黑边 region train/eval GT 不一致
- **根因2**:run.py 默认 `--checkpoint auto` 选了 epoch1(val_loss 最低但欠训练)
- **修复**:① clip_rn_to_sat 也裁 graph_gt.pickle ② 推理用 `--checkpoint epoch:9`
- **结果**:extraction APLS 恢复 0.4211

---

## 四、最终方案

| 场景 | 采样 | 说明 |
|---|---|---|
| 训练 | edge_random | epoch 间稳定,监督均匀 |
| 推理 | component | 真实不破碎,edge_random 训练可泛化 |
| 评测 GT | graph_gt.pickle 也裁剪 | 和训练 GT 一致(都裁黑边) |
| ckpt 选择 | epoch:9 | 不用 auto/best(val_loss 最低≠APLS 最高) |

---

## 五、待办

- [ ] **方案A**:训练无 traj 的 completion ckpt,对比 traj 训练验证 traj 增强价值
- [ ] spacenet 用按边随机重训(当前 component 训的 0.5293)
- [ ] P2CNet spacenet 复现
- [ ] 论文实验章节

---

## 附录:旧实验结果(已作废)

### A.1 didi_xian 0625(裁剪前)

| 档 | APLS | TOPO F1 |
|---|:---:|:---:|
| extraction | 0.4288 | 0.5889 |
| 目标1 | 0.4332 | 0.6081 |
| rn-only(edge_random) | 0.5709 | 0.7728 |
| 目标3(edge_random) | 0.6226 | 0.7833 |

### A.2 didi_xian 0628(裁剪后,epoch1,评测GT未裁)— 作废

| 档 | APLS | 说明 |
|---|:---:|---|
| extraction | 0.2183 | epoch1 欠训练+评测GT未裁,暴跌 |
| rn-only | 0.5808 | |
| full | 0.6099 | |

### A.3 component 采样实验(0626,裁剪前)

| 训练采样 | 推理 | APLS | val_loss |
|---|---|:---:|---|
| edge_random | edge_random | 0.6226 | 平滑 |
| edge_random | component | 0.5984 | 平滑 |
| component | component | 0.4571 | 震荡 |
| component+方案1loss | component | 0.3910 | 震荡 |
